---
phase: 01-transcription-reliability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/services/conversation_engine.dart
  - test/services/conversation_engine_transcription_test.dart
  - ios/Runner/SpeechStreamRecognizer.swift
autonomous: true
requirements: [TRNS-01, TRNS-02]
must_haves:
  truths:
    - "Transcription text continues updating in the UI while an AI response is being generated"
    - "No incoming transcription events are silently dropped during any state"
    - "Audio from glasses PCM path reaches OpenAI/Whisper without local VAD gating"
  artifacts:
    - path: "lib/services/conversation_engine.dart"
      provides: "Transcription pipeline that never drops updates"
      contains: "Always update the transcript UI"
    - path: "test/services/conversation_engine_transcription_test.dart"
      provides: "Regression tests for transcription-during-response behavior"
      min_lines: 50
    - path: "ios/Runner/SpeechStreamRecognizer.swift"
      provides: "Audio routing without local VAD gating"
      contains: "No local VAD gating needed"
  key_links:
    - from: "ios/Runner/SpeechStreamRecognizer.swift"
      to: "openaiTranscriber.appendAudio"
      via: "appendPCMData without VAD gate"
      pattern: "Send all audio to OpenAI"
    - from: "lib/services/conversation_engine.dart"
      to: "_progressiveUpdate"
      via: "onTranscriptionUpdate always calls progressive update"
      pattern: "_progressiveUpdate\\(trimmed\\)"
---

<objective>
Validate and test the transcription streaming fixes that were applied in the current working tree.

Purpose: The fixes removing Dart-side transcription suppression and native-side VAD gating are applied but untested. This plan adds regression tests proving transcription flows during AI response generation, then runs the full validation gate to confirm nothing broke.

Output: Passing test suite covering the transcription-during-response scenario, validated build.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/services/conversation_engine.dart
@test/services/conversation_engine_test.dart
@test/helpers/test_helpers.dart
@ios/Runner/SpeechStreamRecognizer.swift
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add regression tests for transcription-during-response behavior</name>
  <files>test/services/conversation_engine_transcription_test.dart</files>
  <read_first>
    - lib/services/conversation_engine.dart (lines 550-595 — onTranscriptionUpdate method)
    - test/services/conversation_engine_test.dart (lines 1-30 — imports and setup patterns)
    - test/helpers/test_helpers.dart (shared helpers and mock factories)
  </read_first>
  <behavior>
    - Test 1: When _isGeneratingResponse is true, onTranscriptionUpdate still calls _progressiveUpdate and emits transcript snapshot to listeners
    - Test 2: When _isGeneratingResponse is true, onTranscriptionUpdate does NOT schedule transcript analysis (no competing AI calls)
    - Test 3: When _isGeneratingResponse is false, onTranscriptionUpdate clears EvenAI.hasActiveAnswer and schedules analysis normally
    - Test 4: Rapid sequential calls to onTranscriptionUpdate during response generation accumulate text correctly without drops
  </behavior>
  <action>
    Create test/services/conversation_engine_transcription_test.dart with 4 regression tests.

    Use the `.test()` factory from ConversationListeningSession and the mock patterns established in conversation_engine_test.dart. Key setup:
    1. Create a ConversationEngine via the standard test helper pattern
    2. For "during response" tests: set engine's _isGeneratingResponse to true by triggering a question detection flow (call onTranscriptionUpdate with a question, let auto-answer start)
    3. Then call onTranscriptionUpdate with new text and verify the transcript stream emits the update
    4. For the "no analysis scheduled" test: verify that _scheduleTranscriptAnalysis is NOT called when _isGeneratingResponse is true (the early return at line 585)

    Use the existing test helpers — do NOT create new mocks if shared helpers already provide them. Follow the import pattern from conversation_engine_test.dart.
  </action>
  <verify>
    <automated>cd /Users/artjiang/develop/Helix-iOS && flutter test test/services/conversation_engine_transcription_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - File test/services/conversation_engine_transcription_test.dart exists
    - Contains at least 4 test cases covering transcription-during-response scenarios
    - All tests pass with `flutter test test/services/conversation_engine_transcription_test.dart`
    - Tests verify that `onTranscriptionUpdate` emits to UI stream even when `_isGeneratingResponse` is true
  </acceptance_criteria>
  <done>4 regression tests pass, proving transcription updates flow to UI during AI response generation and that analysis is correctly suppressed</done>
</task>

<task type="auto">
  <name>Task 2: Run full validation gate on all applied fixes</name>
  <files>lib/services/conversation_engine.dart, ios/Runner/SpeechStreamRecognizer.swift</files>
  <read_first>
    - CLAUDE.md (validation section — mandatory gate commands)
    - VALIDATION.md (gate details)
  </read_first>
  <action>
    Run the three mandatory validation steps from CLAUDE.md:

    1. `flutter analyze` — must have 0 errors. If warnings exist in the modified files (conversation_engine.dart, SpeechStreamRecognizer.swift), fix them.
    2. `flutter test test/` — all existing tests plus the new transcription tests must pass. If any test fails due to the applied fixes (removed pause/resume, removed VAD gating), update the test to match the new behavior — but only if the test was asserting the OLD broken behavior.
    3. `flutter build ios --simulator --no-codesign` — must build successfully, confirming the Swift changes compile.

    If any step fails, diagnose and fix. The fixes applied are:
    - conversation_engine.dart line 559-561: transcription always flows to UI (was returning early)
    - conversation_engine.dart: removed pauseTranscription/resumeTranscription from auto-answer path
    - SpeechStreamRecognizer.swift line 777-778: Whisper mic sends all audio without VAD check
    - SpeechStreamRecognizer.swift line 1116-1118: OpenAI glasses PCM sends all audio without VAD check
  </action>
  <verify>
    <automated>cd /Users/artjiang/develop/Helix-iOS && flutter analyze && flutter test test/ && flutter build ios --simulator --no-codesign</automated>
  </verify>
  <acceptance_criteria>
    - `flutter analyze` exits with 0 errors
    - `flutter test test/` exits with 0 failures
    - `flutter build ios --simulator --no-codesign` exits with 0 errors
    - No regressions in existing test suite from the transcription fixes
  </acceptance_criteria>
  <done>All three validation gates pass. The streaming fixes (no Dart-side suppression, no native VAD gating) are confirmed safe to ship.</done>
</task>

</tasks>

<verification>
1. `flutter test test/services/conversation_engine_transcription_test.dart` — new regression tests pass
2. `flutter analyze` — 0 errors
3. `flutter test test/` — all tests pass
4. `flutter build ios --simulator --no-codesign` — builds clean
</verification>

<success_criteria>
- 4+ regression tests exist proving transcription flows during AI response generation
- Full validation gate passes with no regressions
- Build compiles successfully with all Swift and Dart changes
</success_criteria>

<output>
After completion, create `.planning/phases/01-transcription-reliability/01-01-SUMMARY.md`
</output>
