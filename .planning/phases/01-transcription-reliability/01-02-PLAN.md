---
phase: 01-transcription-reliability
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - ios/Runner/SpeechStreamRecognizer.swift
  - ios/Runner/OpenAIRealtimeTranscriber.swift
  - ios/Runner/WhisperBatchTranscriber.swift
  - lib/services/settings_manager.dart
autonomous: true
requirements: [TRNS-03, TRNS-04]
must_haves:
  truths:
    - "Apple Cloud segment restart interval is tuned to minimize mid-sentence cuts while preventing speech recognition degradation"
    - "OpenAI Realtime transcriber reconnects automatically on WebSocket drop and resumes transcription"
    - "Whisper batch transcriber produces continuous transcription without chunk boundary gaps"
  artifacts:
    - path: "ios/Runner/SpeechStreamRecognizer.swift"
      provides: "Tuned segment restart with overlap buffer"
      contains: "segmentRestartInterval"
    - path: "ios/Runner/OpenAIRealtimeTranscriber.swift"
      provides: "Reliable WebSocket transcription with reconnect"
      contains: "reconnect"
    - path: "ios/Runner/WhisperBatchTranscriber.swift"
      provides: "Chunk boundary handling for continuous transcription"
      contains: "chunkDurationSec"
  key_links:
    - from: "ios/Runner/SpeechStreamRecognizer.swift"
      to: "restartRecognitionSegment"
      via: "Timer fires at tuned interval"
      pattern: "segmentRestartInterval"
    - from: "ios/Runner/OpenAIRealtimeTranscriber.swift"
      to: "URLSessionWebSocketTask"
      via: "WebSocket reconnect on close/error"
      pattern: "reconnect"
---

<objective>
Tune segment restart timing for Apple Cloud and harden OpenAI Realtime + Whisper batch backends for reliable end-to-end transcription.

Purpose: The streaming fixes from Plan 01 ensure transcription events reach the UI. This plan ensures the three transcription backends produce high-quality, continuous transcription output. Apple Cloud needs segment restart tuning to avoid mid-sentence cuts. OpenAI Realtime needs robust reconnection. Whisper batch needs chunk boundary overlap to prevent word loss.

Output: Tuned and hardened transcription backends ready for device testing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-transcription-reliability/01-01-SUMMARY.md
@ios/Runner/SpeechStreamRecognizer.swift
@ios/Runner/OpenAIRealtimeTranscriber.swift
@ios/Runner/WhisperBatchTranscriber.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tune Apple Cloud segment restart and add overlap buffer</name>
  <files>ios/Runner/SpeechStreamRecognizer.swift</files>
  <read_first>
    - ios/Runner/SpeechStreamRecognizer.swift (lines 70-80 — segmentRestartInterval constant, lines 430-520 — restartRecognitionSegment method)
  </read_first>
  <action>
    The current segment restart interval is 15 seconds. Apple Speech Recognition quality degrades over long segments, but 15s is too aggressive — it cuts mid-sentence frequently.

    Changes to make:

    1. **Increase segment restart interval from 15s to 30s.** Change `segmentRestartInterval` from 15 to 30. Apple Speech handles 30s segments well before degradation; 60s is where quality drops noticeably. 30s halves the number of restart-induced cuts while staying safely under the degradation threshold.

    2. **Add smart restart timing in restartRecognitionSegment().** Before restarting, check if the last partial text ends mid-sentence (no sentence-ending punctuation). If it does, defer restart by up to 5 seconds using a one-shot timer, waiting for a natural sentence boundary. Implementation:
       - Add a `private var deferredRestartTimer: Timer?` property
       - In `restartRecognitionSegment()`, before the restart logic, check if `lastRecognizedText` ends with `.`, `?`, `!`, or is empty
       - If it ends with sentence-ending punctuation or is empty: restart immediately (current behavior)
       - If mid-sentence: schedule a one-shot 5s deferred timer. When the deferred timer fires, restart regardless (hard cap to prevent indefinite deferral)
       - Cancel any pending deferred timer when `stopRecognition()` is called
       - Cancel deferred timer at the start of `restartRecognitionSegment()` to prevent stacking

    3. **Log the restart reason.** Update the log line "Restarting recognition segment" to include whether it was immediate or deferred: `"Restarting recognition segment (immediate — sentence boundary)"` or `"Restarting recognition segment (deferred 5s — mid-sentence)"`.
  </action>
  <verify>
    <automated>cd /Users/artjiang/develop/Helix-iOS && flutter build ios --simulator --no-codesign 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `segmentRestartInterval` is set to 30 (not 15)
    - `deferredRestartTimer` property exists in SpeechStreamRecognizer
    - `restartRecognitionSegment()` checks `lastRecognizedText` for sentence-ending punctuation before restarting
    - `stopRecognition()` cancels `deferredRestartTimer`
    - Build succeeds with `flutter build ios --simulator --no-codesign`
  </acceptance_criteria>
  <done>Segment restart interval is 30s with smart sentence-boundary-aware deferral. Mid-sentence cuts are minimized while preventing Apple Speech degradation.</done>
</task>

<task type="auto">
  <name>Task 2: Harden OpenAI Realtime reconnection and Whisper chunk overlap</name>
  <files>ios/Runner/OpenAIRealtimeTranscriber.swift, ios/Runner/WhisperBatchTranscriber.swift</files>
  <read_first>
    - ios/Runner/OpenAIRealtimeTranscriber.swift (lines 310-400 — stop/reconnect logic, lines 510-540 — reconnectSession, lines 920-940 — retry exhaustion)
    - ios/Runner/WhisperBatchTranscriber.swift (lines 30-40 — chunkDurationSec, lines 100-160 — start and timer logic)
  </read_first>
  <action>
    **OpenAI Realtime hardening:**

    1. Review the existing reconnect logic in `reconnectSession()` (around line 510). Verify it:
       - Increments retry count
       - Uses exponential backoff (1s, 2s, 4s...)
       - Has a max retry limit
       - Resets retry count on successful connection
       If any of these are missing, add them. The reconnect must be robust because WebSocket drops are common on mobile networks.

    2. Add a **stale connection detector**: If no transcription events are received for 30 seconds while audio is being sent (appendAudio is being called), trigger a reconnect. Implementation:
       - Add `private var lastTranscriptionEventTime: Date?` property
       - Update it whenever a transcription event is received from the WebSocket
       - Add `private var lastAudioSendTime: Date?`, update on each `appendAudio` call
       - In the existing ping timer callback (or a new 10s timer), check: if `lastAudioSendTime` is within 10s AND `lastTranscriptionEventTime` is older than 30s, call `reconnectSession()`
       - Log: `"[OpenAITranscriber] Stale connection detected — no events for 30s while sending audio, reconnecting"`

    **Whisper batch chunk overlap:**

    3. Review WhisperBatchTranscriber's chunk boundary handling. The current 5s chunk duration means every 5 seconds, accumulated audio is sent as a batch. Words spoken at the exact chunk boundary can be lost. Add a 0.5s overlap buffer:
       - When the chunk timer fires and sends audio, keep the last 0.5s of audio data in the buffer instead of clearing completely
       - This means the next chunk will include 0.5s of overlap, ensuring boundary words are captured by at least one chunk
       - Add a property: `private let overlapDurationSec: Double = 0.5`
       - When trimming the audio buffer after send, calculate overlap sample count: `Int(sampleRate * overlapDurationSec) * bytesPerSample` and retain that many trailing bytes
       - Log when overlap is retained: `"[WhisperBatch] Retained \(overlapBytes) bytes overlap for next chunk"`
  </action>
  <verify>
    <automated>cd /Users/artjiang/develop/Helix-iOS && flutter build ios --simulator --no-codesign 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - OpenAIRealtimeTranscriber has `lastTranscriptionEventTime` and `lastAudioSendTime` properties
    - Stale connection detection triggers reconnect after 30s of no events while audio is being sent
    - WhisperBatchTranscriber has `overlapDurationSec` property set to 0.5
    - Chunk send retains trailing 0.5s of audio as overlap buffer
    - Build succeeds with `flutter build ios --simulator --no-codesign`
  </acceptance_criteria>
  <done>OpenAI Realtime has stale connection detection with auto-reconnect. Whisper batch has 0.5s chunk overlap to prevent boundary word loss. Both backends are hardened for reliable end-to-end transcription.</done>
</task>

</tasks>

<verification>
1. `flutter build ios --simulator --no-codesign` — full project compiles with all Swift changes
2. `flutter analyze` — no new analyzer errors
3. `flutter test test/` — no regressions from Swift-side changes
4. grep confirms key changes:
   - `grep "segmentRestartInterval.*30" ios/Runner/SpeechStreamRecognizer.swift`
   - `grep "deferredRestartTimer" ios/Runner/SpeechStreamRecognizer.swift`
   - `grep "lastTranscriptionEventTime" ios/Runner/OpenAIRealtimeTranscriber.swift`
   - `grep "overlapDurationSec" ios/Runner/WhisperBatchTranscriber.swift`
</verification>

<success_criteria>
- Segment restart interval is 30s with sentence-boundary-aware deferral
- OpenAI Realtime detects stale connections and auto-reconnects
- Whisper batch retains 0.5s overlap between chunks
- Full validation gate passes (analyze + test + build)
</success_criteria>

<output>
After completion, create `.planning/phases/01-transcription-reliability/01-02-SUMMARY.md`
</output>
