---
phase: 01.1-project-rag-verification-polish-inserted
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/fixtures/project_rag/magic.txt
  - test/fixtures/project_rag/sample.pdf
  - .planning/phases/01.1-project-rag-verification-polish-inserted/GATE_BASELINE.md
  - .planning/phases/01.1-project-rag-verification-polish-inserted/GAPS.md
  - .planning/phases/01.1-project-rag-verification-polish-inserted/evidence/.gitkeep
autonomous: true
requirements: []
must_haves:
  truths:
    - "Fixture TXT exists at test/fixtures/project_rag/magic.txt and contains the exact string 'The magic number is 42'"
    - "Fixture PDF exists at test/fixtures/project_rag/sample.pdf with 2 or 3 pages"
    - "Validation gate (scripts/run_gate.sh) produces a recorded result before any walkthrough begins"
    - "Phase evidence/ directory exists so walkthrough screenshots can be saved into it"
    - "GAPS.md exists as an empty but structured template ready for walkthrough findings"
  artifacts:
    - path: "test/fixtures/project_rag/magic.txt"
      provides: "TXT fixture with canonical unique fact 'The magic number is 42'"
      contains: "The magic number is 42"
    - path: "test/fixtures/project_rag/sample.pdf"
      provides: "2-3 page PDF fixture for PDF upload smoke test"
      min_bytes: 1000
    - path: ".planning/phases/01.1-project-rag-verification-polish-inserted/GATE_BASELINE.md"
      provides: "Pre-walkthrough gate baseline (pass/fail + failing test names if any)"
    - path: ".planning/phases/01.1-project-rag-verification-polish-inserted/GAPS.md"
      provides: "Structured template for walkthrough findings (severity, file:line, repro, action)"
    - path: ".planning/phases/01.1-project-rag-verification-polish-inserted/evidence/.gitkeep"
      provides: "Evidence directory placeholder for screenshots"
  key_links:
    - from: "test/fixtures/project_rag/magic.txt"
      to: "Plan 02 walkthrough TXT upload step"
      via: "file path referenced in walkthrough Task 2"
    - from: "test/fixtures/project_rag/sample.pdf"
      to: "Plan 02 walkthrough PDF upload step"
      via: "file path referenced in walkthrough Task 3"
    - from: ".planning/phases/01.1-.../GATE_BASELINE.md"
      to: "Plan 02 walkthrough triage"
      via: "distinguishes pre-existing failures from walkthrough-introduced regressions"
---

<objective>
Set up the fixtures, gate baseline, and evidence scaffolding required for the Phase 1.1 simulator walkthrough.

Purpose: Plan 02 is a manual simulator walkthrough that needs committed TXT and PDF fixtures with known unique content, a recorded pre-walkthrough gate result to separate pre-existing issues from walkthrough findings, and an evidence/ directory to drop screenshots into. Doing this prep in an autonomous plan lets Plan 02 focus purely on human simulator interaction.

Output: Committed `test/fixtures/project_rag/{magic.txt,sample.pdf}`, a `GATE_BASELINE.md` capturing the gate result, an empty structured `GAPS.md` template, and an `evidence/` directory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.1-project-rag-verification-polish-inserted/01.1-CONTEXT.md
@CLAUDE.md
@VALIDATION.md
@docs/superpowers/plans/2026-04-21-project-rag.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TXT and PDF fixtures for Project RAG smoke test</name>
  <files>test/fixtures/project_rag/magic.txt, test/fixtures/project_rag/sample.pdf</files>
  <read_first>
    - .planning/phases/01.1-project-rag-verification-polish-inserted/01.1-CONTEXT.md (Test Artifacts and Criteria section)
    - docs/superpowers/plans/2026-04-21-project-rag.md lines 4239-4258 (Task 18 smoke-test steps)
  </read_first>
  <action>
    Create two fixture files used by the Phase 1.1 simulator walkthrough.

    (1) Create `test/fixtures/project_rag/magic.txt` (plain UTF-8 text) with 5-10 lines of short narrative content that embeds the EXACT phrase `The magic number is 42` on its own sentence. Surround it with a few filler lines so the file exercises chunking without being trivial. Example content (write exactly this):

    ```
    Project Smoke Test notes.

    This document exists to verify the per-project RAG pipeline end-to-end.
    The magic number is 42.
    It is the canonical answer used by the Phase 1.1 walkthrough probe question.
    Any answer to "What is the magic number?" must cite this line as source [1].
    Unrelated filler sentence about calibration procedures.
    Another filler line to give the chunker more than one chunk boundary to work with.
    ```

    (2) Create `test/fixtures/project_rag/sample.pdf` — a synthetic 2-3 page PDF (content can be lorem-ipsum-like; content doesn't matter for the smoke test, only that it ingests cleanly). Generate it via a one-off Python script using `reportlab` if available (`python3 -c "import reportlab"` test first). If `reportlab` is not available, install it into a temp venv for this generation only — do NOT add `reportlab` to pyproject/requirements.

    The PDF must:
    - Be a valid PDF that opens in Preview.app
    - Have exactly 2 or 3 pages
    - Contain extractable text on each page (not image-only)
    - Be at least 1000 bytes (to ensure non-trivial content)
    - NOT contain the phrase "magic number" (the TXT owns that unique fact; PDF content must be distinct so retrieval citations are unambiguous)

    Suggested PDF content (any 3 short paragraphs of generic prose — "lorem ipsum" is fine). The goal is a file that exercises PDF parsing, not a specific topic.

    After generating, verify both files exist and have correct properties (see acceptance criteria).
  </action>
  <verify>
    <automated>test -f test/fixtures/project_rag/magic.txt && grep -q "The magic number is 42" test/fixtures/project_rag/magic.txt && test -f test/fixtures/project_rag/sample.pdf && test "$(wc -c < test/fixtures/project_rag/sample.pdf | tr -d ' ')" -gt 1000 && file test/fixtures/project_rag/sample.pdf | grep -q "PDF document"</automated>
  </verify>
  <acceptance_criteria>
    - File `test/fixtures/project_rag/magic.txt` exists
    - `grep -c "The magic number is 42" test/fixtures/project_rag/magic.txt` returns `1` (exact phrase present exactly once)
    - `wc -l test/fixtures/project_rag/magic.txt` reports between 5 and 15 lines
    - File `test/fixtures/project_rag/sample.pdf` exists
    - `file test/fixtures/project_rag/sample.pdf` output contains the string `PDF document`
    - `wc -c test/fixtures/project_rag/sample.pdf` reports greater than 1000 bytes
    - `grep -c "magic number" test/fixtures/project_rag/sample.pdf` returns `0` (phrase isolated to TXT)
    - PDF has 2 or 3 pages (verify via `mdls -name kMDItemNumberOfPages test/fixtures/project_rag/sample.pdf` returning 2 or 3, or open in Preview.app)
  </acceptance_criteria>
  <done>Both fixtures committed. TXT contains the canonical magic-number phrase exactly once; PDF is 2-3 pages, over 1000 bytes, extractable text, free of the magic-number phrase.</done>
</task>

<task type="auto">
  <name>Task 2: Run validation gate and record baseline</name>
  <files>.planning/phases/01.1-project-rag-verification-polish-inserted/GATE_BASELINE.md</files>
  <read_first>
    - CLAUDE.md (Validation section — mandatory before any change)
    - VALIDATION.md (Gates 1-6)
    - scripts/run_gate.sh
  </read_first>
  <action>
    Execute the full validation gate and record its result as the Phase 1.1 baseline. This baseline lets the walkthrough (Plan 02) distinguish walkthrough-introduced regressions from pre-existing known issues.

    Steps:

    1. From the repo root, run:
       `bash scripts/run_gate.sh 2>&1 | tee /tmp/helix-gate-01.1-baseline.log`

    2. Capture exit code via `echo $?`

    3. Write `.planning/phases/01.1-project-rag-verification-polish-inserted/GATE_BASELINE.md` with the following structure (fill in real values):

        - Header: `# Phase 1.1 Pre-Walkthrough Gate Baseline`
        - Metadata lines:
          - `**Recorded:** {YYYY-MM-DD HH:MM}`
          - `**Commit:** {short SHA from git rev-parse --short HEAD}`
          - `**Gate script:** scripts/run_gate.sh`
          - `**Exit code:** {0 or 1}`
          - `**Overall status:** {PASS or FAIL}`
        - `## Per-gate results` section with a markdown table of 6 rows (one per gate): Gate name, Result (PASS/FAIL/SKIP), Notes (counts, error names, etc.)
        - `## Known pre-existing issues (NOT caused by Phase 1.1)` section listing:
          - BUG-002 (analytics counter) as a known flake from docs/TEST_BUG_REPORT.md
          - Any other failing test names pulled verbatim from the log
        - `## Implication for walkthrough triage` section: one paragraph stating that any of the above appearing in Plan 02 is a pre-existing issue, not a walkthrough regression.

    4. If the gate FAILS for any NEW reason (a failure not already listed in docs/TEST_BUG_REPORT.md and not BUG-002), STOP and report to the user. Do not commit a broken baseline. Phase execution halts here.

    5. If the gate PASSES (or fails only with the known BUG-002 flake), write the baseline file and commit.

    Keep the full log at `/tmp/helix-gate-01.1-baseline.log` for reference — do NOT commit the raw log.
  </action>
  <verify>
    <automated>test -f .planning/phases/01.1-project-rag-verification-polish-inserted/GATE_BASELINE.md && grep -q "Exit code:" .planning/phases/01.1-project-rag-verification-polish-inserted/GATE_BASELINE.md && grep -q "Overall status:" .planning/phases/01.1-project-rag-verification-polish-inserted/GATE_BASELINE.md && grep -q "Per-gate results" .planning/phases/01.1-project-rag-verification-polish-inserted/GATE_BASELINE.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/01.1-project-rag-verification-polish-inserted/GATE_BASELINE.md` exists
    - Contains line starting with `**Exit code:**` followed by `0` or `1`
    - Contains line starting with `**Overall status:**` followed by `PASS` or `FAIL`
    - Contains line starting with `**Commit:**` followed by a 7+ char hex SHA
    - Contains a `## Per-gate results` section with a table of 6 rows (one per gate)
    - If overall status is FAIL, the `## Known pre-existing issues` section lists every failing test name pulled verbatim from the log
    - Baseline log captured at `/tmp/helix-gate-01.1-baseline.log` (verify with `test -f /tmp/helix-gate-01.1-baseline.log`)
  </acceptance_criteria>
  <done>Gate has been run once against current HEAD. Its result (pass or known-flaky-fail-only) is recorded in GATE_BASELINE.md so Plan 02 can triage findings against a known starting point. Any new, non-BUG-002 failure halts execution and notifies the human.</done>
</task>

<task type="auto">
  <name>Task 3: Scaffold evidence/ directory and GAPS.md template</name>
  <files>.planning/phases/01.1-project-rag-verification-polish-inserted/evidence/.gitkeep, .planning/phases/01.1-project-rag-verification-polish-inserted/GAPS.md</files>
  <read_first>
    - .planning/phases/01.1-project-rag-verification-polish-inserted/01.1-CONTEXT.md (Test Artifacts and Criteria section — deferred findings structure)
  </read_first>
  <action>
    Create the scaffolding Plan 02 (walkthrough) will fill in during execution.

    (1) Create `.planning/phases/01.1-project-rag-verification-polish-inserted/evidence/.gitkeep` as an empty file so the directory is committed.

    (2) Create `.planning/phases/01.1-project-rag-verification-polish-inserted/GAPS.md` containing:

    - Title: `# Phase 1.1 Walkthrough — Gaps and Findings`
    - Intro paragraph: explain that this is a living document appended during the Plan 02 walkthrough; each finding is either a **blocker** (blocks-feature-use, must be fixed in phase) or a **polish** item (UX paper-cut, captured for later).
    - `**Gate baseline:** see GATE_BASELINE.md in this directory.`
    - `**Walkthrough commit:** {filled in by Plan 02 at start}`
    - `## Findings` section with placeholder text "_Append entries below as issues are discovered._"
    - `### Template for each entry` subsection showing the exact format a tester should use for each finding:
      - `#### F-NN — {one-line summary}`
      - `- **Severity:** blocker | polish`
      - `- **Walkthrough step:** {SC number + step description}`
      - `- **Anchor:** {file:line OR "runtime/UI only"}`
      - `- **Repro:** numbered steps`
      - `- **Observed:** ...`
      - `- **Expected:** ...`
      - `- **Recommended next action:** {fix in this phase | fold into Phase 2 | backlog as todo}`
      - `- **Evidence:** evidence/{screenshot.png}`
    - `## Out-of-scope / pre-existing (auto-triaged)` section with "- (none yet)" placeholder. Items matching GATE_BASELINE.md go here.
    - `## Summary (filled at walkthrough end)` section with fields: Blockers: {N}, Polish items: {N}, Pre-existing: {N}, Phase verdict: {CLEAN / GAPS — triggers /gsd:plan-phase 1.1 --gaps}

    Commit both files.
  </action>
  <verify>
    <automated>test -f .planning/phases/01.1-project-rag-verification-polish-inserted/evidence/.gitkeep && test -f .planning/phases/01.1-project-rag-verification-polish-inserted/GAPS.md && grep -q "Phase 1.1 Walkthrough" .planning/phases/01.1-project-rag-verification-polish-inserted/GAPS.md && grep -q "Template for each entry" .planning/phases/01.1-project-rag-verification-polish-inserted/GAPS.md && grep -q "Summary" .planning/phases/01.1-project-rag-verification-polish-inserted/GAPS.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/01.1-project-rag-verification-polish-inserted/evidence/.gitkeep` exists (may be empty)
    - File `.planning/phases/01.1-project-rag-verification-polish-inserted/GAPS.md` exists
    - GAPS.md contains heading `# Phase 1.1 Walkthrough — Gaps and Findings`
    - GAPS.md contains `## Findings` section
    - GAPS.md contains `### Template for each entry` subsection
    - GAPS.md contains `## Out-of-scope / pre-existing (auto-triaged)` section
    - GAPS.md contains `## Summary (filled at walkthrough end)` section with all four fields (Blockers, Polish items, Pre-existing, Phase verdict)
    - GAPS.md references `GATE_BASELINE.md`
  </acceptance_criteria>
  <done>Evidence directory scaffolded; GAPS.md template exists with all required sections and a per-finding template. Plan 02 can start appending findings without deciding on structure.</done>
</task>

</tasks>

<verification>
All three tasks pass their automated verifications. The full command sequence a human can run:

```bash
test -f test/fixtures/project_rag/magic.txt && \
  grep -q "The magic number is 42" test/fixtures/project_rag/magic.txt && \
  test -f test/fixtures/project_rag/sample.pdf && \
  file test/fixtures/project_rag/sample.pdf | grep -q "PDF document" && \
  test -f .planning/phases/01.1-project-rag-verification-polish-inserted/GATE_BASELINE.md && \
  grep -q "Overall status:" .planning/phases/01.1-project-rag-verification-polish-inserted/GATE_BASELINE.md && \
  test -f .planning/phases/01.1-project-rag-verification-polish-inserted/evidence/.gitkeep && \
  test -f .planning/phases/01.1-project-rag-verification-polish-inserted/GAPS.md && \
  echo "Plan 01 artifacts complete"
```

Expected output: `Plan 01 artifacts complete`
</verification>

<success_criteria>
- TXT fixture exists with exact magic-number phrase
- PDF fixture exists, 2-3 pages, non-trivial content, no magic-number phrase
- Gate baseline recorded with exit code, overall status, and per-gate table
- evidence/ directory committed with .gitkeep
- GAPS.md template committed with all required sections
- All files committed to git so Plan 02 sees them on disk
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-project-rag-verification-polish-inserted/01.1-01-SUMMARY.md` documenting:
- Fixture file paths and their properties (line count, page count, byte size)
- Gate baseline result (PASS / FAIL-with-known-flake)
- Any new failures discovered (should be none if baseline passed)
- Confirmation that evidence/ and GAPS.md are ready for Plan 02
</output>
