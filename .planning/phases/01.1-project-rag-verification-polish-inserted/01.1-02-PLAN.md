---
phase: 01.1-project-rag-verification-polish-inserted
plan: 02
type: execute
wave: 2
depends_on: ["01.1-01"]
files_modified:
  - .planning/phases/01.1-project-rag-verification-polish-inserted/GAPS.md
  - .planning/phases/01.1-project-rag-verification-polish-inserted/evidence/
autonomous: false
requirements: ["SC-1", "SC-2", "SC-3", "SC-4", "SC-5"]
must_haves:
  truths:
    - "TXT + PDF upload both transition pending → processing → ready without user intervention (SC-1)"
    - "Ask-this-project returns an answer citing [1] with source text containing '42' (SC-2)"
    - "Selecting an active project persists across app restart, and the home-screen chip reflects the current selection (SC-3)"
    - "During live conversation with active project, answer references project content and citation chips render on the phone; tapping a chip reveals excerpt (SC-4, phone-side only per CONTEXT.md)"
    - "Deleting a project moves it to Recently deleted; undo restores it; restored project survives a restart (SC-5)"
    - "Two sequential probe questions in one session: second turn's citations replace the first turn's, not accumulate (regression check for b6502e1)"
    - "Every blocker or polish finding is recorded in GAPS.md with severity, repro, and evidence path"
  artifacts:
    - path: ".planning/phases/01.1-project-rag-verification-polish-inserted/GAPS.md"
      provides: "Completed walkthrough findings log with final Summary block filled in"
      contains: "Phase verdict:"
    - path: ".planning/phases/01.1-project-rag-verification-polish-inserted/evidence/"
      provides: "Screenshot evidence for each critical transition"
      min_files: 8
  key_links:
    - from: "ActiveProjectChip (lib/screens/home_screen.dart:536)"
      to: "ActiveProjectController.instance state"
      via: "stream subscription; chip text and color reflect selection"
      pattern: "ActiveProjectChip"
    - from: "projectCitationsStream (lib/services/conversation_engine.dart:154-159)"
      to: "Citation chips UI (lib/screens/home_screen.dart:2183)"
      via: "StreamBuilder subscribes to projectCitationsStream"
      pattern: "projectCitationsStream"
    - from: "ConversationEngine._generateResponse (lib/services/conversation_engine.dart:2499-2509)"
      to: "PROJECT CONTEXT system-prompt injection + citation emission"
      via: "ActiveProjectController.instance.active gating + ProjectRagService retrieval"
      pattern: "PROJECT CONTEXT"
---

<objective>
Execute the end-to-end simulator walkthrough of the Project RAG feature on the iPhone 17 Pro simulator (`0D7C3AB2`). Verify all five Phase 1.1 Success Criteria on the phone side, record evidence, and maintain GAPS.md.

Purpose: The Project RAG feature shipped across commits cf57bad..b6502e1 without the final manual smoke test (Task 18 of `docs/superpowers/plans/2026-04-21-project-rag.md`). This plan closes that gap and either produces a clean walkthrough or an actionable GAPS.md that feeds `/gsd:plan-phase 1.1 --gaps`.

Output: Filled-in GAPS.md with findings (or "none"), screenshots in evidence/ for each critical transition, and a final Summary block declaring CLEAN or GAPS verdict.

**This plan is NOT autonomous.** Every task requires a human to drive the simulator UI and visually confirm outcomes. Claude's role is to guide the human step-by-step, write the GAPS.md entries the human dictates, and manage the evidence/ directory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01.1-project-rag-verification-polish-inserted/01.1-CONTEXT.md
@.planning/phases/01.1-project-rag-verification-polish-inserted/GATE_BASELINE.md
@.planning/phases/01.1-project-rag-verification-polish-inserted/GAPS.md
@docs/superpowers/plans/2026-04-21-project-rag.md
@CLAUDE.md

# Key source anchors the walkthrough verifies
@lib/screens/home_screen.dart
@lib/services/conversation_engine.dart
@lib/services/projects/active_project_controller.dart
@lib/services/projects/project_rag_service.dart
</context>

<interfaces>
Key anchors Plan 02 verifies (read-only — this plan does not modify source):

**Home screen integration:**
- `lib/screens/home_screen.dart:536` — `ActiveProjectChip()` in app bar
- `lib/screens/home_screen.dart:2183` — `StreamBuilder(stream: ConversationEngine.instance.projectCitationsStream, ...)` — citation chips beneath live answer
- `lib/screens/home_screen.dart:3645-3677` — text ask-entry with `kDebugMode` seam available

**Engine hook:**
- `lib/services/conversation_engine.dart:154-159` — `_projectCitationsController` + `projectCitationsStream`
- `lib/services/conversation_engine.dart:2499-2509` — `PROJECT CONTEXT` injection + citation emission
- `lib/services/conversation_engine.dart:3484` — stream closed on disposal

**Projects services (under `lib/services/projects/`):**
- `active_project_controller.dart` — singleton, `.instance`, `.active`, `.load()`
- `document_ingest_service.dart` — extract → chunk → embed → persist
- `project_rag_service.dart` — cosine retrieval, returns `RetrievedChunk[]`
- `project_context_formatter.dart` — builds `PROJECT CONTEXT` preamble

**Projects UI:**
- `lib/screens/projects/projects_list_screen.dart`
- `lib/screens/projects/project_detail_screen.dart`
- `lib/screens/projects/project_ask_dialog.dart`
</interfaces>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Boot dedicated simulator and launch Helix debug build</name>
  <files>(runtime only)</files>
  <read_first>
    - CLAUDE.md ("Build" section and simulator rules)
    - .planning/phases/01.1-project-rag-verification-polish-inserted/GATE_BASELINE.md
  </read_first>
  <action>
    Prepare the walkthrough environment.

    1. Record the current HEAD SHA in `GAPS.md` by replacing `{filled in by Plan 02 at start}` with the output of `git rev-parse --short HEAD`.

    2. Instruct the user to boot the dedicated Helix simulator:
       - `xcrun simctl boot 0D7C3AB2` (iPhone 17 Pro, the Helix-reserved slot per CLAUDE.md)
       - Confirm no other app is running on it (it is the Helix-reserved slot)

    3. Launch Helix in debug on that simulator:
       - `flutter run -d 0D7C3AB2`
       - Wait for app to fully boot (Home tab visible)

    4. Confirm OpenAI API key is configured in Settings:
       - Tap Settings tab
       - Verify "OpenAI API Key" field shows a value (masked is fine). If blank, user enters their key and saves.

    5. Capture screenshot `evidence/01-launch-home.png` showing the Home tab with the ActiveProjectChip visible in the app bar.

    **Resume signal:** User confirms "app running on 0D7C3AB2, API key configured, home tab visible with ActiveProjectChip"
  </action>
  <what-built>Plan 01 fixtures committed, gate baseline recorded — nothing new built in this task; this is environment prep.</what-built>
    <verify>
    <automated>test -f .planning/phases/01.1-project-rag-verification-polish-inserted/evidence/01-launch-home.png && grep -qE 'Walkthrough commit: [0-9a-f]{6,}' .planning/phases/01.1-project-rag-verification-polish-inserted/GAPS.md</automated>
    <human-verify>See &lt;how-to-verify&gt; below for visual confirmation steps — the automated check above confirms only that evidence was written.</human-verify>
  </verify>
  <how-to-verify>
    1. Run `xcrun simctl list | grep 0D7C3AB2` — state should be `Booted`
    2. Helix app is visible on the simulator home screen in debug mode
    3. Settings tab shows a populated OpenAI API key
    4. `evidence/01-launch-home.png` exists
  </how-to-verify>
  <resume-signal>Type "app-ready" or describe any launch issue</resume-signal>
  <acceptance_criteria>
    - `xcrun simctl list | grep 0D7C3AB2` reports state `Booted`
    - Flutter debug build is running (pid visible via `ps -ef | grep flutter`, or `flutter run` terminal still active)
    - Settings shows OpenAI API key configured (non-empty)
    - `evidence/01-launch-home.png` exists and shows ActiveProjectChip
    - GAPS.md `**Walkthrough commit:**` line now contains a real SHA (grep for `Walkthrough commit: [0-9a-f]`)
  </acceptance_criteria>
  <done>Simulator booted on 0D7C3AB2, debug Helix build running, API key configured, initial screenshot captured, walkthrough commit SHA recorded in GAPS.md.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: SC-1 — Create "Smoke Test" project and upload TXT fixture; verify pending → processing → ready</name>
  <files>.planning/phases/01.1-project-rag-verification-polish-inserted/evidence/02-txt-ready.png, .planning/phases/01.1-project-rag-verification-polish-inserted/GAPS.md</files>
  <read_first>
    - docs/superpowers/plans/2026-04-21-project-rag.md lines 4244-4246 (Task 18 steps for project create + TXT upload)
    - test/fixtures/project_rag/magic.txt (committed in Plan 01)
  </read_first>
  <action>
    Verify SC-1 (TXT half): TXT upload transitions pending → processing → ready without user intervention.

    User steps:

    1. Tap the **Projects** tab (3rd sub-tab in live history per CONTEXT.md).
    2. Tap "New project" (or equivalent create button per `lib/screens/projects/projects_list_screen.dart`).
    3. Enter project name exactly: `Smoke Test`. Tap create.
    4. Open the newly created project. The project detail screen loads.
    5. Tap the Upload / Add document button.
    6. Drag or select `test/fixtures/project_rag/magic.txt` into the simulator. On macOS simulator, use `xcrun simctl addmedia 0D7C3AB2 test/fixtures/project_rag/magic.txt` OR use the Files app's "Add" flow if the doc picker is wired to iCloud Drive. (If the doc picker does NOT see the fixture, that's a blocker — record in GAPS.md and continue with a TXT created inline via the Files app.)
    7. Watch the document status. It should transition through:
       - `pending` (immediately visible)
       - `processing` (extraction + chunking + embedding)
       - `ready` (terminal state)
    8. Capture screenshot `evidence/02-txt-ready.png` showing the document row in `ready` state.

    **If a transition stalls > 60 seconds:** record as a blocker in GAPS.md:
    - F-NN, Severity: blocker, Walkthrough step: SC-1 (TXT upload)
    - Anchor: lib/services/projects/document_ingest_service.dart (pipeline likely hung in extract/embed)
    - Repro: upload magic.txt, observe status stuck at {pending|processing}
    - Evidence: screenshot of the stalled state

    **If the upload succeeds but the status never reaches `ready`:** same blocker structure, different anchor.

    **If success:** do NOT append to GAPS.md for this step. Proceed.
  </action>
  <what-built>TXT upload round-trip exercises projects_service, document_ingest_service (extract + chunk + embed), project_dao persistence, and UI status polling.</what-built>
    <verify>
    <automated>test -f .planning/phases/01.1-project-rag-verification-polish-inserted/evidence/02-txt-ready.png</automated>
    <human-verify>See &lt;how-to-verify&gt; below for visual confirmation steps — the automated check above confirms only that evidence was written.</human-verify>
  </verify>
  <how-to-verify>
    1. In the simulator, Projects tab shows "Smoke Test" project
    2. Project detail screen shows `magic.txt` document with status `ready`
    3. Screenshot `evidence/02-txt-ready.png` exists and visually confirms `ready` status
    4. No blocker appended to GAPS.md for this step (or if appended, tester has confirmed the finding is accurate)
  </how-to-verify>
  <resume-signal>Type "txt-ready" (success) or "blocker: {description}" (failure)</resume-signal>
  <acceptance_criteria>
    - Project named exactly `Smoke Test` exists (visible in Projects tab)
    - Document `magic.txt` is in `ready` status on the project detail screen
    - `evidence/02-txt-ready.png` exists
    - If any failure: GAPS.md contains a new F-NN entry with severity=blocker, anchor pointing to a file under `lib/services/projects/`, and evidence path populated
  </acceptance_criteria>
  <done>SC-1 (TXT half) verified: upload transitions pending → processing → ready on simulator. Evidence captured. Findings recorded if any.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: SC-1 — Upload PDF fixture; verify pending → processing → ready</name>
  <files>.planning/phases/01.1-project-rag-verification-polish-inserted/evidence/03-pdf-ready.png, .planning/phases/01.1-project-rag-verification-polish-inserted/GAPS.md</files>
  <read_first>
    - docs/superpowers/plans/2026-04-21-project-rag.md line 4247 (Task 18 PDF step)
    - test/fixtures/project_rag/sample.pdf (committed in Plan 01)
  </read_first>
  <action>
    Verify SC-1 (PDF half): PDF upload transitions pending → processing → ready without user intervention.

    User steps:

    1. From the Smoke Test project detail screen, tap Upload again.
    2. Select `test/fixtures/project_rag/sample.pdf`. Use `xcrun simctl addmedia 0D7C3AB2 test/fixtures/project_rag/sample.pdf` to make it available if the picker doesn't see it.
    3. Watch the status. Expect: pending → processing → ready.
    4. Note the page count shown in the document row (should be 2 or 3 — matches fixture).
    5. Capture `evidence/03-pdf-ready.png`.

    **Failure modes to watch for:**
    - PDF parse error (status goes to `failed` or stalls): blocker, anchor `lib/services/projects/document_ingest_service.dart` PDF extract path
    - Page count mismatch (shows 1 page when fixture is 2-3): polish, anchor same file
    - No progress indicator during processing: polish (UX), not blocker

    Record findings per template.
  </action>
  <what-built>PDF upload exercises the PDF-specific extraction path (likely `pdf_text` or similar) + same chunking/embedding pipeline as TXT.</what-built>
    <verify>
    <automated>test -f .planning/phases/01.1-project-rag-verification-polish-inserted/evidence/03-pdf-ready.png</automated>
    <human-verify>See &lt;how-to-verify&gt; below for visual confirmation steps — the automated check above confirms only that evidence was written.</human-verify>
  </verify>
  <how-to-verify>
    1. `sample.pdf` document visible on Smoke Test detail screen, status `ready`
    2. Page count shown matches fixture (2 or 3)
    3. `evidence/03-pdf-ready.png` exists
  </how-to-verify>
  <resume-signal>Type "pdf-ready" or "blocker: {description}"</resume-signal>
  <acceptance_criteria>
    - `sample.pdf` is in `ready` status on the Smoke Test project detail screen
    - Page count shown is 2 or 3
    - `evidence/03-pdf-ready.png` exists
    - SC-1 is now fully verified (both TXT and PDF upload work end-to-end)
    - Any failures recorded in GAPS.md with correct severity and anchor
  </acceptance_criteria>
  <done>SC-1 fully verified. PDF upload works end-to-end on simulator.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: SC-2 — Ask-this-project returns answer citing [1] with magic-number source</name>
  <files>.planning/phases/01.1-project-rag-verification-polish-inserted/evidence/04-ask-cited.png, .planning/phases/01.1-project-rag-verification-polish-inserted/GAPS.md</files>
  <read_first>
    - docs/superpowers/plans/2026-04-21-project-rag.md line 4248 (Task 18 Ask step)
    - lib/screens/projects/project_ask_dialog.dart
    - lib/services/projects/project_rag_service.dart
    - lib/services/projects/project_context_formatter.dart
  </read_first>
  <action>
    Verify SC-2: Ask-this-project returns `[1]` citation pointing to magic.txt content containing "42".

    User steps:

    1. On the Smoke Test project detail screen, tap **Ask this project**.
    2. In the probe dialog, type exactly: `What is the magic number?`
    3. Tap Ask / Send.
    4. Observe the response. It must:
       - Contain the digits `42` in the answer text
       - Include citation `[1]` (bracketed numeric citation)
       - Show a source excerpt (either inline or via tap) matching content from `magic.txt` — specifically the line containing "The magic number is 42"
    5. Capture `evidence/04-ask-cited.png` showing the answer with `[1]` visible.

    **Acceptance bar:**
    - `[1]` appears in the answer text (visually OR via the source panel)
    - Tapping `[1]` (or the citation chip if present) reveals the excerpt from magic.txt
    - Excerpt contains the string "42" or "magic number is 42"

    **Failure modes:**
    - No citation rendered even though answer is correct → blocker, anchor `lib/screens/projects/project_ask_dialog.dart` citation rendering
    - Citation present but excerpt from wrong document (the PDF) → blocker, anchor `lib/services/projects/project_rag_service.dart` retrieval/ranking
    - Answer does not mention 42 at all → investigate: either retrieval returned no chunks (blocker, anchor rag service) or the LLM ignored context (polish, anchor `project_context_formatter.dart`)
  </action>
  <what-built>Ask-this-project exercises the full retrieval path: embed query → cosine search over project vectors → top-K chunks → format as context → LLM call → render citations.</what-built>
    <verify>
    <automated>test -f .planning/phases/01.1-project-rag-verification-polish-inserted/evidence/04-ask-cited.png</automated>
    <human-verify>See &lt;how-to-verify&gt; below for visual confirmation steps — the automated check above confirms only that evidence was written.</human-verify>
  </verify>
  <how-to-verify>
    1. Ask dialog shows response text that contains "42"
    2. `[1]` citation is visible (inline bracket OR citation chip)
    3. Tapping the citation reveals excerpt from magic.txt containing "magic number is 42"
    4. `evidence/04-ask-cited.png` captures the cited answer
  </how-to-verify>
  <resume-signal>Type "ask-cited" or "blocker: {description}"</resume-signal>
  <acceptance_criteria>
    - Ask response contains the digits `42`
    - Ask response contains `[1]` citation marker
    - Citation excerpt (revealed via tap or inline) contains "magic number is 42" or equivalent substring
    - `evidence/04-ask-cited.png` exists and shows the cited answer
    - SC-2 verified OR a blocker with correct anchor recorded in GAPS.md
  </acceptance_criteria>
  <done>SC-2 verified. Ask-this-project correctly cites the TXT content for the magic-number question.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: SC-3 — Active project selection persists across restart; home chip reflects state</name>
  <files>.planning/phases/01.1-project-rag-verification-polish-inserted/evidence/05a-chip-no-project.png, .planning/phases/01.1-project-rag-verification-polish-inserted/evidence/05b-chip-smoke-test.png, .planning/phases/01.1-project-rag-verification-polish-inserted/evidence/05c-after-restart.png, .planning/phases/01.1-project-rag-verification-polish-inserted/GAPS.md</files>
  <read_first>
    - docs/superpowers/plans/2026-04-21-project-rag.md lines 4249-4250 (Task 18 chip + restart step)
    - lib/screens/home_screen.dart:536 (ActiveProjectChip)
    - lib/services/projects/active_project_controller.dart
  </read_first>
  <action>
    Verify SC-3: ActiveProjectChip reflects state, selecting a project persists across restart.

    User steps:

    1. Navigate to the **Home** tab.
    2. Verify the ActiveProjectChip in the app bar shows "No project" (since nothing has been selected as active yet, even though Smoke Test exists).
    3. Capture `evidence/05a-chip-no-project.png`.
    4. Tap the chip. A picker appears listing projects.
    5. Select **Smoke Test**.
    6. Verify the chip now reads "Smoke Test" (or shows the project indicator).
    7. Capture `evidence/05b-chip-smoke-test.png`.
    8. Kill the app entirely: swipe up and remove Helix from the multitasking tray. (In simulator: use the app switcher or `xcrun simctl terminate 0D7C3AB2 {bundle-id}` — bundle ID is `com.helix.hololens` per MEMORY.md.)
    9. Relaunch the app (tap icon, OR re-run `flutter run -d 0D7C3AB2`).
    10. Once Home loads, verify the ActiveProjectChip STILL shows "Smoke Test" (persisted via ActiveProjectController.load()).
    11. Capture `evidence/05c-after-restart.png`.

    **Failure modes:**
    - Chip shows wrong state after selection → blocker, anchor `lib/services/projects/active_project_controller.dart` or `lib/screens/home_screen.dart:536`
    - Chip resets to "No project" after restart → blocker, anchor `active_project_controller.dart` `load()` + `lib/main.dart` init order
    - Chip is invisible or missing → blocker, anchor `lib/screens/home_screen.dart:536`
  </action>
  <what-built>Verifies ActiveProjectController persistence + subscription wiring (main.dart loads before runApp; Home subscribes; chip updates reactively).</what-built>
    <verify>
    <automated>test -f .planning/phases/01.1-project-rag-verification-polish-inserted/evidence/05a-chip-no-project.png && test -f .planning/phases/01.1-project-rag-verification-polish-inserted/evidence/05b-chip-smoke-test.png && test -f .planning/phases/01.1-project-rag-verification-polish-inserted/evidence/05c-after-restart.png</automated>
    <human-verify>See &lt;how-to-verify&gt; below for visual confirmation steps — the automated check above confirms only that evidence was written.</human-verify>
  </verify>
  <how-to-verify>
    1. Initial chip state: "No project"
    2. After selection: "Smoke Test"
    3. After full app kill + relaunch: still "Smoke Test"
    4. All three screenshots captured
  </how-to-verify>
  <resume-signal>Type "chip-persisted" or "blocker: {description}"</resume-signal>
  <acceptance_criteria>
    - `evidence/05a-chip-no-project.png` shows chip reading "No project"
    - `evidence/05b-chip-smoke-test.png` shows chip reading "Smoke Test" after selection
    - `evidence/05c-after-restart.png` shows chip still reading "Smoke Test" after full app restart
    - Bundle ID `com.helix.hololens` was terminated and relaunched between 05b and 05c (not just backgrounded)
    - SC-3 verified OR blocker recorded with anchor in active_project_controller.dart / home_screen.dart:536
  </acceptance_criteria>
  <done>SC-3 verified. Active project persists across restart; chip reflects state.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: SC-4 — Live conversation with active project; answer references content + citation chips render (phone-side)</name>
  <files>.planning/phases/01.1-project-rag-verification-polish-inserted/evidence/06a-live-answer-cited.png, .planning/phases/01.1-project-rag-verification-polish-inserted/evidence/06b-chip-excerpt.png, .planning/phases/01.1-project-rag-verification-polish-inserted/GAPS.md</files>
  <read_first>
    - docs/superpowers/plans/2026-04-21-project-rag.md lines 4251-4254 (Task 18 live conversation step)
    - .planning/phases/01.1-project-rag-verification-polish-inserted/01.1-CONTEXT.md (Live-Session Phone-Side Verification Protocol section)
    - lib/screens/home_screen.dart:2183 (citation chips StreamBuilder)
    - lib/services/conversation_engine.dart:154-159 (projectCitationsStream)
    - lib/services/conversation_engine.dart:2499-2509 (PROJECT CONTEXT injection)
    - lib/screens/home_screen.dart:3645 (text ask-entry seam — _askController)
  </read_first>
  <action>
    Verify SC-4 (phone-side only — glasses HUD rendering deferred per CONTEXT.md).

    **Probe injection choice:** Per CONTEXT.md, the preferred seam order is:
    1. Existing HomeScreen text ask-entry (`_askController` at home_screen.dart:3645) if it routes through ConversationEngine with active-project gating
    2. Voice mic in simulator (macOS mic routes through)
    3. kDebugMode text-injection helper (add one-line helper only if #1 and #2 fail)

    User steps:

    1. On Home tab with "Smoke Test" as active project, trigger a live question via the text ask-entry: type `What is the magic number?` and send.
    2. Observe:
       - **(a)** Probe question appears in transcript UI (or equivalent "you asked" indicator)
       - **(b)** Streamed answer renders in the live answer area
       - **(c)** Answer text contains `42` (or references the magic number)
       - **(d)** Citation chips appear beneath the answer (home_screen.dart:2183 StreamBuilder region)
       - **(e)** Tap a citation chip — excerpt from magic.txt is revealed (dialog, sheet, or inline expansion)
    3. Capture `evidence/06a-live-answer-cited.png` showing the answer with chips visible.
    4. Capture `evidence/06b-chip-excerpt.png` showing the tap-revealed excerpt.

    **Debug log check (confirms PROJECT CONTEXT injection):**
    - Tail flutter debug console during the probe
    - Expect a log line containing `PROJECT CONTEXT` emitted from `conversation_engine.dart` around line 2499-2509
    - If log is absent, injection hook is broken → blocker, anchor `conversation_engine.dart:2499-2509`

    **Triage rule (per CONTEXT.md):**
    - If answer streams correctly but NO citations appear → blocker, anchor `home_screen.dart:2183` StreamBuilder OR `conversation_engine.dart:154-159` stream emission
    - If answer doesn't stream at all → Phase 1/2 territory (transcription or LLM pipeline); record in GAPS.md as "out-of-scope for 1.1, refer to Phase 1/2" and continue
    - If citations fire but excerpts are wrong → blocker, anchor `project_rag_service.dart` or `project_context_formatter.dart`

    **If text ask-entry at home_screen.dart:3645 does NOT route through the active-project path:** record as polish "no convenient debug seam for live RAG probe" (NOT a blocker — the feature itself works via voice; this is tester ergonomics), then fall back to voice mic to complete the SC-4 verification.
  </action>
  <what-built>Verifies the full live-conversation RAG integration: active project gating → retrieval → PROJECT CONTEXT injection → citation emission → UI stream subscription → chip rendering → excerpt reveal.</what-built>
    <verify>
    <automated>test -f .planning/phases/01.1-project-rag-verification-polish-inserted/evidence/06a-live-answer-cited.png && test -f .planning/phases/01.1-project-rag-verification-polish-inserted/evidence/06b-chip-excerpt.png</automated>
    <human-verify>See &lt;how-to-verify&gt; below for visual confirmation steps — the automated check above confirms only that evidence was written.</human-verify>
  </verify>
  <how-to-verify>
    1. Live answer area shows answer containing "42"
    2. Citation chips visible beneath the answer
    3. Tapping a chip reveals excerpt from magic.txt
    4. Flutter console log contains "PROJECT CONTEXT" line during probe
    5. Both screenshots captured
  </how-to-verify>
  <resume-signal>Type "live-cited" (success), "out-of-scope: {description}" (pipeline failure not owned by 1.1), or "blocker: {description}"</resume-signal>
  <acceptance_criteria>
    - Answer text contains `42`
    - Citation chip is visible beneath the answer (screenshot 06a)
    - Tapping the chip reveals excerpt text containing "magic number is 42" or "The magic number" (screenshot 06b)
    - Flutter debug console showed `PROJECT CONTEXT` log during the probe (user confirms OR console snippet is pasted into GAPS.md)
    - SC-4 phone-side verified OR blocker/out-of-scope recorded with correct triage per CONTEXT.md rules
    - Glasses-HUD rendering explicitly deferred (do NOT attempt)
  </acceptance_criteria>
  <done>SC-4 phone-side verified. Live conversation with active project injects PROJECT CONTEXT, answer references project content, citation chips render, tap reveals excerpt. Glasses HUD deferred.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 7: Regression check for commit b6502e1 — two sequential probes, citations replace not accumulate</name>
  <files>.planning/phases/01.1-project-rag-verification-polish-inserted/evidence/07-citations-replaced.png, .planning/phases/01.1-project-rag-verification-polish-inserted/GAPS.md</files>
  <read_first>
    - .planning/phases/01.1-project-rag-verification-polish-inserted/01.1-CONTEXT.md (the b6502e1 regression check)
    - lib/services/conversation_engine.dart (search for the fix that clears stale citation chips at start of each response turn)
  </read_first>
  <action>
    Verify commit b6502e1 ("clear stale citation chips at start of each response turn") has not regressed.

    User steps (in the same session as Task 6):

    1. After Task 6's answer + chips are still visible, send a SECOND probe: type `What color is the sky?` (or any question that does NOT match magic.txt content).
    2. Observe the new answer streams in.
    3. **Critical check:** the citation chips beneath the new answer should REPLACE the previous ones, not accumulate. Specifically:
       - If the second answer has NO citations (expected — question doesn't match project content), the chip area should be EMPTY, not still showing the magic-number chip from probe 1.
       - If the second answer DOES have citations (edge case), they should be only the ones for probe 2.
    4. Capture `evidence/07-citations-replaced.png` showing the second probe's answer area with either no chips or only probe-2 chips.

    **Failure mode:** chips from probe 1 still visible beneath probe 2's answer → blocker, regression of b6502e1, anchor the file in `lib/services/conversation_engine.dart` that contains the stale-clear logic (search for the commit message fix).

    **Alternative test if second probe returns a "smoke test"-related answer:** ask a THIRD probe about something truly unrelated (e.g., `What year is it?`). The key is ensuring at least one turn with no expected citations follows a turn with citations.
  </action>
  <what-built>Validates commit b6502e1's fix: citations cleared at the start of each response turn, preventing cross-turn chip leak.</what-built>
    <verify>
    <automated>test -f .planning/phases/01.1-project-rag-verification-polish-inserted/evidence/07-citations-replaced.png</automated>
    <human-verify>See &lt;how-to-verify&gt; below for visual confirmation steps — the automated check above confirms only that evidence was written.</human-verify>
  </verify>
  <how-to-verify>
    1. Two sequential probes executed in same session
    2. First probe has magic-number chip(s); second probe (non-matching question) has empty chip area or only probe-2 chips
    3. `evidence/07-citations-replaced.png` captures the second probe's answer + empty/replaced chip area
  </how-to-verify>
  <resume-signal>Type "regression-passed" or "regression-failed: {description}"</resume-signal>
  <acceptance_criteria>
    - Two probes executed in one session (no app restart between them)
    - Second probe's chip area does NOT show probe 1's citations (visually verified)
    - `evidence/07-citations-replaced.png` shows second probe's answer area with zero or replaced chips only
    - If failed: GAPS.md contains a blocker entry flagging b6502e1 regression with file anchor
  </acceptance_criteria>
  <done>b6502e1 regression check complete. Citations correctly clear between turns (or regression recorded).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 8: SC-5 — Delete project, verify Recently deleted, undo, restart-persistence</name>
  <files>.planning/phases/01.1-project-rag-verification-polish-inserted/evidence/08a-recently-deleted.png, .planning/phases/01.1-project-rag-verification-polish-inserted/evidence/08b-after-undo.png, .planning/phases/01.1-project-rag-verification-polish-inserted/evidence/08c-restored-after-restart.png, .planning/phases/01.1-project-rag-verification-polish-inserted/GAPS.md</files>
  <read_first>
    - docs/superpowers/plans/2026-04-21-project-rag.md lines 4255-4257 (Task 18 delete + restore steps)
    - lib/services/projects/projects_service.dart (soft delete + purge)
    - lib/services/database/project_dao.dart
  </read_first>
  <action>
    Verify SC-5: delete → Recently deleted → undo → survives restart.

    User steps:

    1. Navigate to the Smoke Test project detail screen.
    2. Delete the project (button in detail screen header or settings sheet).
    3. Confirm deletion.
    4. Expect:
       - Project disappears from main Projects list
       - ActiveProjectChip on Home falls back to "No project" (since active was Smoke Test)
       - A "Recently deleted" section (or tab/filter) in the Projects screen now lists "Smoke Test"
    5. Capture `evidence/08a-recently-deleted.png` showing Smoke Test in Recently deleted.
    6. Tap **Undo** (or Restore) on the Smoke Test row in Recently deleted.
    7. Expect: project returns to the main Projects list.
    8. Capture `evidence/08b-after-undo.png` showing Smoke Test back in the main list.
    9. Kill the app fully (same method as Task 5: `xcrun simctl terminate 0D7C3AB2 com.helix.hololens` OR swipe-close).
    10. Relaunch. Navigate to Projects tab.
    11. Expect: Smoke Test still in the main list (restoration persisted).
    12. Capture `evidence/08c-restored-after-restart.png`.

    **Failure modes:**
    - Project vanishes permanently (no Recently deleted entry) → blocker, anchor `lib/services/projects/projects_service.dart` soft-delete path
    - ActiveProjectChip doesn't fall back to "No project" after the active project is deleted → blocker, anchor `active_project_controller.dart` delete-subscription
    - Undo doesn't restore → blocker, anchor `projects_service.dart` restore
    - Restore doesn't persist across restart → blocker, anchor DAO soft-delete flag write
  </action>
  <what-built>Verifies soft-delete + restore flow, chip fallback on active-project deletion, and persistence of restore across restart.</what-built>
    <verify>
    <automated>test -f .planning/phases/01.1-project-rag-verification-polish-inserted/evidence/08a-recently-deleted.png && test -f .planning/phases/01.1-project-rag-verification-polish-inserted/evidence/08b-after-undo.png && test -f .planning/phases/01.1-project-rag-verification-polish-inserted/evidence/08c-restored-after-restart.png</automated>
    <human-verify>See &lt;how-to-verify&gt; below for visual confirmation steps — the automated check above confirms only that evidence was written.</human-verify>
  </verify>
  <how-to-verify>
    1. After delete: project in Recently deleted, not main list; chip says "No project"
    2. After undo: project back in main list
    3. After restart: project still in main list
    4. All three screenshots captured
  </how-to-verify>
  <resume-signal>Type "delete-restore-persisted" or "blocker: {description}"</resume-signal>
  <acceptance_criteria>
    - `evidence/08a-recently-deleted.png` shows Smoke Test in a "Recently deleted" section and ActiveProjectChip reading "No project"
    - `evidence/08b-after-undo.png` shows Smoke Test back in the main Projects list
    - `evidence/08c-restored-after-restart.png` shows Smoke Test in main list after full app kill + relaunch
    - SC-5 verified OR blocker with correct anchor recorded
  </acceptance_criteria>
  <done>SC-5 verified. Delete → Recently deleted → undo → restart-persists flow works end-to-end.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 9: Finalize GAPS.md Summary and declare phase verdict</name>
  <files>.planning/phases/01.1-project-rag-verification-polish-inserted/GAPS.md</files>
  <read_first>
    - .planning/phases/01.1-project-rag-verification-polish-inserted/GAPS.md (findings accumulated through Tasks 1-8)
    - .planning/phases/01.1-project-rag-verification-polish-inserted/GATE_BASELINE.md
  </read_first>
  <action>
    Finalize the walkthrough record.

    1. Read GAPS.md. Count entries in each category:
       - Blockers (severity: blocker)
       - Polish items (severity: polish)
       - Pre-existing / out-of-scope (entries matching GATE_BASELINE.md known issues)

    2. Fill in the `## Summary (filled at walkthrough end)` block with real numbers:
       - Blockers: {N}
       - Polish items: {N}
       - Pre-existing: {N}
       - Phase verdict: **CLEAN** if blockers == 0, otherwise **GAPS**

    3. If verdict is **GAPS**, also add a final paragraph: `Next step: run `/gsd:plan-phase 1.1 --gaps` to generate a gap-closure plan.`

    4. If verdict is **CLEAN**, add: `Next step: mark Phase 1.1 complete in ROADMAP.md. Proceed to Phase 2.`

    5. Verify evidence/ directory contains all expected screenshots:
       - 01-launch-home.png
       - 02-txt-ready.png
       - 03-pdf-ready.png
       - 04-ask-cited.png
       - 05a-chip-no-project.png, 05b-chip-smoke-test.png, 05c-after-restart.png
       - 06a-live-answer-cited.png, 06b-chip-excerpt.png
       - 07-citations-replaced.png
       - 08a-recently-deleted.png, 08b-after-undo.png, 08c-restored-after-restart.png

    6. If any screenshots missing, note in GAPS.md Summary as "Evidence incomplete: {list}" (polish severity, does not change verdict but must be acknowledged).

    7. Commit GAPS.md with updated Summary block and all evidence files.
  </action>
  <what-built>Final triage and verdict for the walkthrough. Produces the single source of truth for "is the feature ready to ship?"</what-built>
    <verify>
    <automated>grep -qE 'Phase verdict: (CLEAN|GAPS)' .planning/phases/01.1-project-rag-verification-polish-inserted/GAPS.md && [ $(ls .planning/phases/01.1-project-rag-verification-polish-inserted/evidence/*.png 2>/dev/null | wc -l) -ge 12 ]</automated>
    <human-verify>See &lt;how-to-verify&gt; below for visual confirmation steps — the automated check above confirms only that evidence was written.</human-verify>
  </verify>
  <how-to-verify>
    1. GAPS.md Summary section has all four fields populated with real numbers (not `{N}`)
    2. Phase verdict is either CLEAN or GAPS
    3. Next-step line is present and correct
    4. `ls .planning/phases/01.1-project-rag-verification-polish-inserted/evidence/*.png | wc -l` returns at least 12
    5. Commit made with GAPS.md + evidence files
  </how-to-verify>
  <resume-signal>Type "verdict: CLEAN" or "verdict: GAPS"</resume-signal>
  <acceptance_criteria>
    - `grep -E "Blockers: [0-9]+" GAPS.md` matches
    - `grep -E "Polish items: [0-9]+" GAPS.md` matches
    - `grep -E "Pre-existing: [0-9]+" GAPS.md` matches
    - `grep -E "Phase verdict: (CLEAN|GAPS)" GAPS.md` matches exactly one of the two values
    - If verdict is GAPS: GAPS.md contains the string `/gsd:plan-phase 1.1 --gaps`
    - If verdict is CLEAN: GAPS.md contains the string `Phase 2`
    - `ls .planning/phases/01.1-project-rag-verification-polish-inserted/evidence/ | wc -l` reports >= 12 PNG files (or gap acknowledged in Summary)
    - Git commit was made including GAPS.md and all evidence/ PNGs
  </acceptance_criteria>
  <done>GAPS.md finalized with real counts and verdict. Evidence committed. Next step is unambiguously stated.</done>
</task>

</tasks>

<verification>
Phase-level verification at the end:

```bash
# Confirm GAPS.md is finalized
grep -E "Phase verdict: (CLEAN|GAPS)" \
  .planning/phases/01.1-project-rag-verification-polish-inserted/GAPS.md && \
echo "verdict recorded"

# Confirm evidence directory is populated
ls .planning/phases/01.1-project-rag-verification-polish-inserted/evidence/*.png 2>/dev/null | wc -l
# Expect: >= 12

# Confirm commit landed
git log --oneline -5 | grep -i "phase 1.1\|01.1\|walkthrough"
```

Expected: verdict string present, >= 12 PNG files, commit visible in git log.
</verification>

<success_criteria>
- All five Success Criteria (SC-1 through SC-5) either verified with evidence or recorded as blockers in GAPS.md
- Regression check for b6502e1 passed or recorded as blocker
- SC-4 verified phone-side only (glasses HUD explicitly deferred)
- GAPS.md Summary block filled with real counts and a verdict (CLEAN or GAPS)
- Next-step instruction is unambiguous (`/gsd:plan-phase 1.1 --gaps` OR "proceed to Phase 2")
- Evidence/ contains at least 12 screenshots
- Everything committed to git
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-project-rag-verification-polish-inserted/01.1-02-SUMMARY.md` documenting:
- Final verdict (CLEAN or GAPS) and counts
- Per-SC result (SC-1 through SC-5: passed / blocker / polish)
- Regression check result (b6502e1)
- Evidence inventory (list of screenshot files)
- Any out-of-scope findings deferred to Phase 1, 2, or backlog
- Recommended next workflow invocation
</output>
